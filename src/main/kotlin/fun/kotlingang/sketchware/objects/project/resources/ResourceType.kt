package `fun`.kotlingang.sketchware.objects.project.resources

import `fun`.kotlingang.sketchware.interfaces.objects.Identifiable

enum class ResourceType(override val id: Int) : Identifiable {
    DEFAULT(1)
}