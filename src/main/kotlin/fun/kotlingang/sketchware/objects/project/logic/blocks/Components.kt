package `fun`.kotlingang.sketchware.objects.project.logic.blocks

sealed interface ComponentBlock : ReturnableBlock

sealed interface BluetoothComponentBlock : ComponentBlock

sealed interface CalendarComponentBlock : ComponentBlock

sealed interface CameraComponentBlock : ComponentBlock

sealed interface DialogComponentBlock : ComponentBlock

sealed interface FilePickerComponentBlock : ComponentBlock

sealed interface FirebaseAuthComponentBlock : ComponentBlock

sealed interface FirebaseDatabaseComponentBlock : ComponentBlock

sealed interface FirebaseStorageComponentBlock : ComponentBlock

sealed interface GyroscopeComponentBlock : ComponentBlock

sealed interface IntentComponentBlock : ComponentBlock

sealed interface InterstitialAdComponentBlock : ComponentBlock

sealed interface LocationManagerComponentBlock : ComponentBlock

sealed interface MediaPlayerComponentBlock : ComponentBlock

sealed interface ObjectAnimatorComponentBlock : ComponentBlock

sealed interface RequestNetworkComponentBlock : ComponentBlock

sealed interface SharedPreferencesComponentBlock : ComponentBlock

sealed interface SoundPoolComponentBlock : ComponentBlock

sealed interface SpeechToTextComponentBlock : ComponentBlock

sealed interface TextToSpeechComponentBlock : ComponentBlock

sealed interface TimerComponentBlock : ComponentBlock

sealed interface VibratorComponentBlock : ComponentBlock